# Custom partition table for the XIAO ESP32-S3 Sense (8 MB flash).
# The default single-factory table caps the app at ~1 MB, too small once the
# WiFi stack, HTTP server, and esp32-camera driver are linked in. This gives
# the factory app 3 MB, which the camera+wifi+http build fits comfortably.
#
# Name,   Type, SubType,  Offset,   Size
nvs,      data, nvs,      0x9000,   0x6000
phy_init, data, phy,      0xf000,   0x1000
factory,  app,  factory,  0x10000,  0x300000
